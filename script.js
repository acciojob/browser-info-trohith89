//your JS code here. If required.
let browserName = navigator.appName;
let version = navigator.appVersion;
document.getElementById("container").innerHTML =  "You are using " + browserName + " version " + version;